import styled from 'styles/styled-components';

const H2 = styled.h2`
  font-size: 25px;
  margin-top: 0px;
  margin-bottom: 0px;
  font-family: 'Meta', sans-serif;
  padding-top: 0px;
  font-weight: normal;
`;

export default H2;
